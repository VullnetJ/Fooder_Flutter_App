
import 'package:flutter/material.dart';

class MockFooderService extends StatelessWidget {
  const MockFooderService({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}